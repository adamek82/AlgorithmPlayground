{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Launch AlgorithmPlayground (auto)",
            "type": "cppvsdbg",  // ✅ Use MSVC debugger
            "request": "launch",
            "program": "${workspaceFolder}/bin/${command:cmake.buildType}/AlgorithmPlayground.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
        }
    ]
}
